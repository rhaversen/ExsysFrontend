apiVersion: apps/v1
kind: Deployment
metadata:
    name: frontend-deployment
    namespace: exsys-production
spec:
    replicas: 2
    strategy:
        type: RollingUpdate
        rollingUpdate:
            maxUnavailable: 1
            maxSurge: 1
    selector:
        matchLabels:
            app: frontend-deployment
    template:
        metadata:
            labels:
                app: frontend-deployment
        spec:
            automountServiceAccountToken: false
            containers:
                - name: frontend-deployment
                  resources:
                      limits:
                          cpu: "0.1"
                          memory: "100Mi"
                      requests:
                          cpu: "0.05"
                          memory: "50Mi"
                  image: "rhaversen/exsys-frontend:${GITHUB_SHA}"
                  imagePullPolicy: Always
                  livenessProbe:
                      httpGet:
                          path: service/livez
                          port: 5000
                      periodSeconds: 10
                      initialDelaySeconds: 15
                      failureThreshold: 5
                  readinessProbe:
                      httpGet:
                          path: service/readyz
                          port: 5000
                      periodSeconds: 10
                      initialDelaySeconds: 15
                      failureThreshold: 5
                  ports:
                      - containerPort: 5000
                  envFrom:
                      - secretRef:
                            name: frontend-secrets
